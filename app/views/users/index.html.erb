<ul>
  <%= will_paginate @users %>
  
  <% @users.each do |user| %>
    <li><%= link_to  user.name, user%></li>
  <%end%>
</ul>

<div class="mx-auto text-center my-4">
  <%= will_paginate @users %>
</div>