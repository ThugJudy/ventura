
<%@domains = ['AI','ML','cloud','Physics']  %>

<%=form_with url: ideas_filtered_path do |form|%>
  <%= form.select :dom, 
                    options_for_select(@domains),
                    {},
                    {:multiple => true, :class =>'block w-full pl-3 pr-10 py-2 text-green-800 placeholder-gray-400 transition duration-100 ease-in-out bg-black border border-blue-900 rounded shadow-sm focus:border-blue-500 focus:ring-2 focus:ring-blue-500 focus:outline-none focus:ring-opacity-50 disabled:opacity-50 disabled:cursor-not-allowed',} %>
  <%= form.submit "search"%>

<%end%>
